name: Docker Image 

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build & Push the Docker image
      run: | 
        docker image build -t Ghazi-Meftah/tether:latest .
        docker push Ghazi-Meftah/tether:latest
